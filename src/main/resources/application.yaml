sackbot:
  bot:
    token: bot_token
    activity:
      type: watching
      text: hottest memes
    chat:
      enabled: true
      allowDm: true
      commandPrefix: /
  nitrite:
    dbFile: ${user.home}${file.separator}sackbot${file.separator}database.db
